---
title: 函数的方法
createTime: 2026/03/31 16:51:23
permalink: /qa/js/c2f82i9i/
---

下面的代码执行结果是什么？

<JSRunner :code="code" />

### 1. 执行前的内存状态

```javascript
function Foo() {
  // 修改静态方法（当Foo被调用时才执行）
  Foo.a = function () {
    console.log(1);
  };
  // 创建实例方法
  this.a = function () {
    console.log(2);
  };
}

// 在原型上添加方法
Foo.prototype.a = function () {
  console.log(3);
};

// 定义静态方法（立即执行）
Foo.a = function () {
  console.log(4);
};
```

此时：

- `Foo.a` 指向 `function() { console.log(4); }`（静态方法）
- `Foo.prototype.a` 指向 `function() { console.log(3); }`（原型方法）

### 2. 第一次调用 `Foo.a()`

```javascript
Foo.a(); // 输出 4
```

直接调用构造函数上的静态方法 `Foo.a`。

### 3. 执行 `new Foo()`

```javascript
let obj = new Foo();
```

`new` 操作执行构造函数 `Foo()`：

1. **执行 `Foo.a = function() { console.log(1); }`**
   - 修改了静态方法 `Foo.a` 的指向
   - 现在 `Foo.a` 指向 `function() { console.log(1); }`
2. **执行 `this.a = function() { console.log(2) }`**
   - 为实例 `obj` 添加实例方法 `a`

此时内存状态：

```javascript
// 静态方法（已被修改）
Foo.a = function () {
  console.log(1);
};

// 实例方法
obj = {
  a: function () {
    console.log(2);
  },
  // __proto__ 指向 Foo.prototype
};

// 原型方法（保持不变）
Foo.prototype.a = function () {
  console.log(3);
};
```

### 4. 调用 `obj.a()`

```javascript
obj.a(); // 输出 2
```

查找顺序（原型链）：

1. 先查找 `obj` 自身的属性：找到实例方法 `a()` → 执行输出 `2`
2. **不会**查找到原型上的 `a()`，因为自身已有该方法

### 5. 第二次调用 `Foo.a()`

```javascript
Foo.a(); // 输出 1
```

调用的是已被构造函数修改后的静态方法。

### 验证原型链

```javascript
// 验证原型链方法
console.log(obj.__proto__.a()); // 3（原型方法仍然存在）

// 删除实例方法后
delete obj.a;
console.log(obj.a()); // 3（现在使用原型方法）
console.log(obj.a === Foo.prototype.a); // true

// 验证静态方法只能通过构造函数访问
console.log(obj.a === Foo.a); // false（一个是实例/原型方法，一个是静态方法）
console.log(Foo.a()); // 1
```

### 关键点

- **静态方法 vs 实例方法 vs 原型方法** 是三个不同的东西
- 构造函数内部的 `Foo.a = ...` 修改的是**静态方法**（函数执行时修改）
- `this.a = ...` 创建的是**实例方法**（覆盖同名原型方法）
- 实例方法查找优先级：**实例属性 > 原型属性**

<script setup>
const code = `function Foo(){
  Foo.a = function(){
    console.log(1);
  }
  // 注释掉下面定义实例方法后再运行试试
  this.a = function(){
    console.log(2)
  }
}
Foo.prototype.a = function(){
  console.log(3);
}
Foo.a = function(){
  console.log(4);
}
Foo.a();
let obj = new Foo();
obj.a();
Foo.a();`
</script>